# frontend-build-e2e-auditor handoff

## Scope
Phase 1.6.1 Task A (build/e2e truth) + Task E carry-over verification for the WTC Ecosystem Platform monorepo (`C:\Users\maxib\GTE BOT\wtc_ecosystem_platform`). READ-ONLY audit. Goal: predict whether `npm run build -w @wtc/web` (expect "Compiled successfully", 31/31 pages) and `npm run e2e` (expect 10/10 desktop+mobile) will pass cleanly on a freshly-cleaned tree, and surface any real app bug vs stale-artifact poisoning BEFORE the operator runs the two gates SEQUENTIALLY after cleaning artifacts. Phase 1.6 changed two UI files: `apps/web/src/app/admin/tradingview-access/page.tsx` (TV-access memory-backed label) and `apps/web/src/app/(app)/app/bots/page.tsx` (added a `BOT_ADAPTER_MODE=mock` "Simulated data" banner via `RiskWarningBanner`). All conclusions below are VERIFIED against files read this session unless explicitly marked INHERITED.

## Files inspected
- `playwright.config.ts` (full)
- `tests/e2e/smoke.spec.ts` (full, 61 lines — the ONLY e2e spec)
- `apps/web/src/app/(app)/app/bots/page.tsx` (full, 59 lines — changed page #1)
- `apps/web/src/app/admin/tradingview-access/page.tsx` (full, 68 lines — changed page #2)
- `apps/web/src/app/(public)/page.tsx` (full, 53 lines — the landing page the prior failure was on)
- `apps/web/src/components/PublicTopBar.tsx` (full, 25 lines)
- `packages/ui/src/components.tsx` (full, 109 lines — `RiskWarningBanner` def at line 56)
- `packages/ui/src/index.ts` (full, 14 lines — barrel re-export of `RiskWarningBanner` at line 9)
- root `package.json` (scripts) + `apps/web/package.json` (`dev:e2e = next dev --port 3100`)
- Prior failure artefacts (READ): `test-results/.last-run.json`, `test-results/smoke-public-landing-renders-desktop/error-context.md`, `.../test-failed-1.png` (viewed), and the `trace.zip` listing
- Filesystem mtimes: `apps/web/.next` (and its manifest tree), the three source pages, `test-results/*`, `STATUS.md`
- Context/governance: `AGENTS.md`, `docs/SESSION_PROTOCOL.md`, `docs/handoffs/0000-orchestrator-seed.md`, `docs/STATUS.md`, `docs/IMPLEMENTED_FILES.md`, `docs/handoffs/20260529-2052-phase-1-6-enforcement-persistence-truth.md` (aggregate), `docs/handoffs/20260529-2052-ui-product-truth-auditor.md`, `docs/handoffs/20260529-2052-qa-ci-gates-auditor.md` (the flake disclosure)

## Files changed
None — read-only audit

## Findings

1. **e2e webServer uses `next dev --port 3100` (NODE_ENV=development), NOT a prebuilt production server -> build and e2e are INDEPENDENT; stale production `.next` cannot poison e2e.** Severity: info (positive). Evidence: `playwright.config.ts:20-26` -- `webServer.command='npm run dev:e2e -w @wtc/web'`, `url:'http://localhost:3100'`, `timeout:150_000`, `reuseExistingServer:!process.env.CI`; `apps/web/package.json` `"dev:e2e":"next dev --port 3100"`; top comment `playwright.config.ts:3`. e2e compiles routes on demand; it never reads `apps/web/.next` production output. A stale/dirty/missing production `.next` cannot cause a false pass or block e2e. Confidence: HIGH. Recommendation: none (safe design); see finding 10 re `reuseExistingServer`.

2. **e2e spec inventory = exactly ONE file `tests/e2e/smoke.spec.ts` with 5 `test(...)` cases; 2 projects (desktop 1440x900, mobile 390x844); workers=1 -> 5x2 = 10 total. Confirmed.** Severity: info. Evidence (VERIFIED, full spec read): the 5 tests are:
   - `test('public landing renders')` -> `goto('/')`; asserts `getByRole('heading',{level:1})` visible + `getByText('Every')` visible; `page.screenshot('landing')`. (spec:14-19)
   - `test('pricing renders')` -> `goto('/pricing')`; asserts `getByText('Plans & bundles')`; screenshot `pricing`. (spec:21-25)
   - `test('/products/legacy-bot renders (slug matches SITEMAP)')` -> `goto('/products/legacy-bot')`; `expect(res.status()).toBeLessThan(400)` + heading `'Legacy Bot'`. (spec:27-31)
   - `test('user dashboard, bot warnings, terminal, security')` -> `login('user@wtc.local')`, heading `'Account overview'`; **`goto('/app/bots/tortila')`** asserts `getByText('Risk & audit warnings')` + `getByText('TP reconciliation / restore not implemented')`; then `/app/terminal` (heading `'Axioma Terminal'`), `/app/security` (heading `'Exchange keys & security'`); screenshots each. (spec:33-50)
   - `test('admin console')` -> `login('admin@wtc.local')`, `/admin/entitlements` (heading `'Entitlements'`); **`goto('/admin/tradingview-access')`** asserts `getByRole('heading',{name:'TradingView access queue'})`; screenshots. (spec:52-61)
   `playwright.config.ts:7-8,11-19` confirm `workers:1`, `fullyParallel:false`, `baseURL:'http://localhost:3100'`, `screenshot:'only-on-failure'`, `trace:'retain-on-failure'`, projects desktop+mobile. Total = 10. Confidence: HIGH. Recommendation: operator confirms reporter prints "Running 10 tests using 1 worker".

3. **The two changed pages ARE exercised by the e2e suite, but the changed TEXT is NOT asserted against — so the Phase 1.6 banner/label edits CANNOT break any selector.** Severity: info (positive — directly answers the brief). Evidence (VERIFIED):
   - The bots LIST page (`/app/bots`, where the new "Simulated data" banner was added) is **NOT visited at all** by the suite — the spec only navigates to `/app/bots/tortila` (the DETAIL page, spec:38). So the new list-page banner is invisible to e2e. The detail-page assertions (`'Risk & audit warnings'`, `'TP reconciliation / restore not implemented'`, spec:39-40) target pre-existing warning text untouched by Phase 1.6.
   - The TV-access page (`/admin/tradingview-access`) IS visited (spec:58) but the ONLY assertion is `getByRole('heading',{name:'TradingView access queue'})` (spec:59) — the page's `SectionHeader title="TradingView access queue"` (`tradingview-access/page.tsx:38`) is unchanged. The new `storage: in-memory (demo)` pill + memory-backed caption (`page.tsx:39-42`) are NOT asserted.
   - `screenshot:'only-on-failure'` (`playwright.config.ts:13`) + the explicit `page.screenshot()` calls write artefact PNGs but are NOT `toHaveScreenshot()` pixel-diff assertions, so the new banner text does not fail any visual baseline.
   Conclusion: NO selector update is needed for either Phase 1.6 edit. Confidence: HIGH. Recommendation: none.

4. **bots page: `RiskWarningBanner` import resolves AND the `{severity,title,detail}` prop shape is exactly correct (`severity:'info'|'warning'|'error'`; `"warning"` is valid). No type/JSX error.** Severity: info (positive). Evidence (VERIFIED, both sides read): call site `apps/web/src/app/(app)/app/bots/page.tsx:6` imports `RiskWarningBanner` from `@wtc/ui`; usage `:31-37` passes `severity="warning" title="Simulated data — not a live account" detail="Bot dashboards use the mock adapter (BOT_ADAPTER_MODE=mock)..."`; barrel `packages/ui/src/index.ts:9` re-exports it; definition `packages/ui/src/components.tsx:56` is `export function RiskWarningBanner({ severity, title, detail }: { severity: 'info' | 'warning' | 'error'; title: string; detail: string })`. Literal `"warning"` is in the union; `title`/`detail` are strings. Every link in the import chain matches. The same component is used identically on `bots/[bot]/page.tsx`. Confidence: HIGH. Recommendation: none — the earlier "prop mismatch" risk is fully eliminated.

5. **bots page JSX/TS is valid and the `botAdapterMode() === 'mock'` gate is correct.** Severity: info (positive). Evidence (VERIFIED): well-formed async Server Component, single root `<div className="wtc-stack">`, balanced tags, banner guarded by `botAdapterMode() === 'mock' &&` (`:31`); `botAdapterMode`/`botAdapterOptions` from `@/lib/server-config` (`:5`), `getBotAdapter` from `@wtc/bot-adapters` (`:4`), `healthTone()`->`Tone` (`:13-15`); `&&` short-circuit yields `false` (valid React child) when not mock. No type error. Confidence: HIGH. Recommendation: none.

6. **tradingview-access page: corrected memory-backed label present, valid TSX, introduces NO new symbol -> near-zero build risk.** Severity: info (positive). Evidence (VERIFIED): `apps/web/src/app/admin/tradingview-access/page.tsx:39-42` renders `<StatusPill tone="warn">storage: in-memory (demo)</StatusPill>` + caption "This admin queue is memory-backed; DB-persisted TV repos exist + are worker-tested, but the web UI is NOT yet wired to them — Postgres wiring deferred (Part E)." (matches the ui-product-truth-auditor's exact recommended string and STATUS.md:26,53). Server actions carry `'use server'` and run `requireUser()`->`assertAdmin(actor.roles)`->`await assertCsrf(formData)` before mutating; balanced JSX; all imports pre-existing. Phase 1.6 edit was string-only. Confidence: HIGH. Recommendation: none.

7. **KEY TASK-A DELIVERABLE — prior FAILURE artefact READ and CLASSIFIED: it is a KNOWN, ALREADY-FIXED FLAKE on the `public landing` test (first-compile timeout), NOT a real app bug and NOT related to either changed page.** Severity: medium (artefact is a STALE failure that the operator's clean step must delete so it doesn't mislead). Evidence (VERIFIED):
   - `test-results/.last-run.json` = `{ "status": "failed", "failedTests": ["4219922fea2e2bd3c691-c90441813aa94b1f7fed"] }` — exactly ONE failed test.
   - The failure dir is `test-results/smoke-public-landing-renders-desktop/` -> the failed test is **`public landing renders` on the DESKTOP project** (spec:14-19, `goto('/')`). This is the FIRST test in the suite and hits the dev server's cold first-compile path.
   - `error-context.md` is a Playwright **page-snapshot of a FULLY-RENDERED landing page**: it shows the `banner`, the `heading "Operate every WTC product from one terminal." [level=1]`, the lead paragraph, product list (Tortila Bot link), and `text: demo environment`. i.e. the page DID render correctly — the failure was a timing/visibility timeout, not a missing element or a crash. `test-failed-1.png` (viewed) likewise shows the normal landing page.
   - **Root cause confirmed in writing:** `docs/handoffs/20260529-2052-qa-ci-gates-auditor.md:120` — "e2e wall time pushed the public-landing test's networkidle wait over its timeout once; a re-run was green. The dev server's first compile is the slow path." So this is a documented one-off flake; the re-run went green and STATUS.md:36 + the Phase 1.6 aggregate gates table both record the authoritative result as **e2e 10/10**.
   - The landing page (`apps/web/src/app/(public)/page.tsx`, mtime 2026-05-29 16:26) was **NOT touched by Phase 1.6** (the two changed pages are mtime 20:46). The flake is wholly unrelated to the audit scope.
   - **Stale-vs-current timeline (mtimes):** build output `apps/web/.next` = 22:07:47; the failure artefacts were written immediately after at `error-context.md` 22:08:34 / `trace.zip` 22:08:35 / `.last-run.json` 22:08:36. So this `test-results/` set is from a LATER ad-hoc e2e run (22:08) than the Phase-1.6 STATUS write (21:58) — it is a fresh-but-flaky single run, the public-landing first-compile timeout recurring, NOT a regression from the UI edits.
   Verdict: **NO real app bug.** The artefact is a stale flaky-run residue; on a clean re-run it goes green (per the auditor's own note). Recommendation: the operator's clean step MUST remove `test-results/` (and `.last-run.json`) so this stale "failed" status cannot be misread as a current failure; and, given finding 8, the operator should be ready to tolerate/auto-retry exactly this first-compile flake.

8. **REAL RESIDUAL RISK to the e2e gate: the `public landing` first-compile timeout flake can recur, and the suite has `retries: 0` + a 90s test timeout against a `next dev` cold start.** Severity: medium (the single most likely way `npm run e2e` reports < 10/10 on a clean tree — and it is a flake, not a code defect). Evidence (VERIFIED): `playwright.config.ts` sets `timeout: 90_000`, `expect.timeout: 15_000`, and NO `retries` key (defaults to 0). The webServer gets `timeout: 150_000` to boot, but the FIRST test (`public landing`, spec:15 `goto('/')` with default `waitUntil:'load'`) still races the dev server's first on-demand route compile; the existing `test-results` failure + the qa-ci-gates-auditor note (`:120`) prove this has tripped at least twice historically. Because there are no retries, one slow first compile = one failed test = a `9/10`-style result that is purely environmental. Confidence: HIGH that the flake exists; the qa auditor logged it as LOW and "re-run was green", so it is intermittent. Recommendation: operator should (a) expect a possible first-run flake on `public landing` and simply re-run `npm run e2e` (the documented green path), or (b) warm the server / accept Playwright's reuse. This does NOT change the "code is correct" verdict — it is a known infra flake, not a Phase 1.6 regression.

9. **`apps/web/.next` exists from a prior build (mtime 22:07:47) — expected, NOT poisoning; the clean step removes it and e2e ignores it anyway.** Severity: low. Evidence (VERIFIED): `.next/server/app/...` compiled `page.js` present for both changed routes + others; full manifest tree timestamped ~22:07-22:08. Moot for e2e (finding 1) and regenerated by build. Confidence: HIGH. Recommendation: ensure clean removes `apps/web/.next`.

10. **`reuseExistingServer: !process.env.CI` -> a leftover dev server on :3100 could serve STALE code locally.** Severity: low. Evidence (VERIFIED): `playwright.config.ts:25`. Locally (CI unset), Playwright reuses any running :3100 server. If a stale `dev:e2e` holds :3100 with old code, the suite tests stale code. Recommendation: ensure nothing holds :3100 before `npm run e2e`, or export `CI=1` for a guaranteed-fresh server (this also disables reuse, forcing the cold-compile path of finding 8 — trade-off).

11. **Build "31/31 pages" is consistent: both edited routes were already in the last build output; neither edit adds/removes a route.** Severity: info. Evidence (VERIFIED via the `.next/server/app` tree): both changed routes already compiled; both Phase 1.6 edits are string/JSX-only inside existing route files. Confidence: MEDIUM-HIGH (count not independently recomputed). Recommendation: confirm the build summary prints 31 routes.

12. **Doc cross-reference nit:** the orchestrator prompt cited `docs/handoffs/20260529-2052-frontend-product-truth-auditor.md`; no such 2052 file exists (only `20260529-1921-frontend-product-truth-auditor.md`); the 2052 UI-truth work is in `20260529-2052-ui-product-truth-auditor.md` (read). Severity: info (likely a prompt typo). Recommendation: none.

## Decisions
- Treat build and e2e as INDEPENDENT gates: e2e runs against `next dev` on :3100 (`playwright.config.ts:20-26`), immune to stale production `.next`. (VERIFIED.)
- Classify the on-disk `test-results` "failed" status as a STALE, documented first-compile flake on the (unchanged) public-landing test — NOT a real bug and NOT a Phase 1.6 regression (finding 7), with the recurrence risk called out as finding 8.
- Run NO gate/build/server/install/artefact-mutating command (read-only mandate).

## Risks
- **First-compile flake on `public landing` (finding 8) — MEDIUM, environmental:** with `retries:0`, a slow `next dev` cold start can fail test #1 and yield `<10/10` on an otherwise-correct tree. Mitigation: re-run `npm run e2e` (documented green) or warm the server. This is the single most likely cause of a non-green e2e result and it is NOT a code defect.
- **Stale `test-results` "failed" status — MEDIUM if not cleaned:** if the operator does NOT delete `test-results/` first, the leftover `.last-run.json:status=failed` + `error-context.md`/`trace.zip` could be misread as a current failure. The brief says the operator cleans artefacts; ensure that includes `test-results/`.
- **Prop-type mismatch — ELIMINATED** (finding 4): `RiskWarningBanner` `severity` union includes `'warning'`; call site matches `{severity,title,detail}`.
- **Selector breakage from the Phase 1.6 edits — NONE** (finding 3): the new banner text is on a page e2e never visits (`/app/bots` list), and the TV page's only assertion is its unchanged heading.
- **`reuseExistingServer` stale-server** (finding 10): LOW; mitigated by killing :3100 / `CI=1` (but `CI=1` forces the cold-compile path, finding 8).
- Platform remains NOT production-ready (unchanged) — orthogonal to build/e2e.

## Verification/tests
Gates RUN this session: NONE (read-only audit; forbidden from running build/e2e/dev/coverage/install/artefact-mutating commands).
Static verification performed (VERIFIED): full reads of `playwright.config.ts`, `tests/e2e/smoke.spec.ts`, both changed pages, the landing page + `PublicTopBar`, `packages/ui/src/components.tsx` + `index.ts`, both `package.json` script blocks; read of all prior-failure artefacts (`.last-run.json`, `error-context.md`, `test-failed-1.png`, `trace.zip` listing); mtime comparison of `.next` / source pages / `test-results`; and the qa-ci-gates-auditor flake disclosure (`20260529-2052-qa-ci-gates-auditor.md:120`).
INHERITED (relied on, not re-run by me): STATUS.md:33 `typecheck -w @wtc/web -> PASS`; STATUS.md:35 `build -w @wtc/web -> PASS (31/31)`; STATUS.md:36 + Phase 1.6 aggregate gates table `e2e -> 10/10` — all on the final tree already containing the two edits.

## Next actions
1. Operator: clean artefacts — MUST remove `test-results/` (clears the stale `failed` status, finding 7) and `apps/web/.next` (+ any `playwright-report/`) — then run `npm run build -w @wtc/web`, then `npm run e2e` SEQUENTIALLY. Confirm build prints "Compiled successfully" + 31 routes; Playwright prints "Running 10 tests using 1 worker" -> 10 passed.
2. If e2e reports a single `public landing` failure: it is the documented first-compile flake (findings 7,8) — simply re-run `npm run e2e`; do NOT treat it as a Phase 1.6 regression. Optional durable fix (out of scope here): add `retries: process.env.CI ? 2 : 1` to `playwright.config.ts`, or `await page.goto('/', { waitUntil: 'domcontentloaded' })`.
3. Before e2e, decide on :3100: kill any leftover dev server (or `CI=1` for a fresh server — noting `CI=1` forces the cold-compile path that triggers finding 8).
4. No code change needed for either Phase 1.6 page; both are build/typecheck-safe and not asserted-against destructively (findings 3-6).

## Prediction
PASS predicted for BOTH gates on a clean tree, with one caveat.
- **Build -> "Compiled successfully", 31/31 pages: HIGH confidence.** Both edited files are valid TSX (findings 5,6); `RiskWarningBanner`'s export + exact `{severity:'info'|'warning'|'error';title;detail}` prop type is VERIFIED against the `severity="warning"` call site (finding 4); the TV page is string-only with no new symbol; build is decoupled from any stale artefact (finding 9). No real build risk identified.
- **e2e -> 10/10 desktop+mobile: MEDIUM-HIGH confidence, with one known FLAKE caveat.** The suite is a single `smoke.spec.ts` (5 tests x 2 projects), runs against a fresh `next dev` (no stale-`.next` poisoning, finding 1), and the two Phase 1.6 edits touch nothing the suite asserts on (finding 3). The ONLY realistic way to see `<10/10` on this clean tree is the documented first-compile timeout on the `public landing` desktop test (findings 7,8) — an environmental flake, NOT a code defect, that goes green on re-run. 
- **Stale-artifact verdict:** the on-disk `test-results` "failed" status is a STALE, already-classified flake on the UNCHANGED public-landing page (full error text + trace read; root cause = dev-server first-compile timeout per qa-ci-gates-auditor:120). It is NOT a real app bug and NOT related to the Phase 1.6 changes. Provided the operator's clean step removes `test-results/`, there is no poisoning risk.
NO real app bug identified anywhere in scope. The two changed pages are build- and typecheck-safe and selector-safe.
